{
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn frecha_api.wsgi:application --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/health-simple/",
    "healthcheckTimeout": 60
  }
}
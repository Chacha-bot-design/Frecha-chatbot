{
  "deploy": {
    "startCommand": "python manage.py migrate --noinput && gunicorn frecha_api.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/health/",
    "healthcheckTimeout": 300
  }
}
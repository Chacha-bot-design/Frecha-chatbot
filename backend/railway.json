{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py migrate && gunicorn frecha_api.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckPath": "/api/health/",
    "healthcheckTimeout": 300
  }
}